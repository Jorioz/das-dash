# das-dash (gateway)

Currently uses MrDIYs ESP32 CAN Bus Shield with a ESP32 Dev Module.

Acts as a server fulfilling requests from client. Connects via OBD2 port, sending CAN Bus messages and relaying the responses back to the client via ESP-NOW.
